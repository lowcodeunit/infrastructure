name: {{Name}}

on:
  push:
    branches: [{{Branches}}]
  pull_request:
    branches: [{{Branches}}]

permissions: write-all

env:
  CI: false
  Branches: {{Branches}}
  MainBranch: {{MainBranch}}
  Output: {{Output}}
name: Docker Build and Push

on: 
  push:
    branches: main

env:
  APP_NAME: {{IMAGE_NAME}}
